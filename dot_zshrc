export ZSH="/home/derek/.oh-my-zsh"
export PATH=$PATH:$HOME/bin
# ZSH_THEME="robbyrussell"
export _JAVA_AWT_WM_NONREPARENTING=1

# plugins=(git)
plugins=(git npm zsh-autosuggestions z zsh-syntax-highlighting)
source $ZSH/oh-my-zsh.sh
alias em='emacsclient --create-frame --alternate-editor=vim'
alias node-scripts="(cat package.json 2>/dev/null || cat ../package.json 2>/dev/null || cat ../../package.json) | jq .scripts"
# export EDITOR='emacsclient -t'
# export VISUAL='emacsclient -c -a emacs'
export EDITOR='vim'
export VISUAL='vim'
alias caf='chezmoi apply --force'
alias ca='chezmoi apply'
alias cdiff='chezmoi diff'
alias ccd='chezmoi cd'
alias cedit='chezmoi edit'

# function preexec() {
  # __TIMER=$EPOCHREALTIME
# }
# 
# function powerline_precmd() {
  # local __ERRCODE=$?
  # local __DURATION=0
# 
  # if [ -n $__TIMER ]; then
    # local __ERT=$EPOCHREALTIME
    # __DURATION="$(($__ERT - ${__TIMER:-__ERT}))"
  # fi
	# eval "$(powerline-go -eval -modules-right duration -modules ssh,cwd,perms,git,newline,jobs,exit,root -duration $__DURATION -error $__ERRCODE -error $? -jobs ${${(%):%j}:-0})"
	# unset timer
# }
# 
# function install_powerline_precmd() {
	# for s in "${precmd_functions[@]}"; do
		# if [ "$s" = "powerline_precmd" ]; then
			# return
		# fi
	# done
	# precmd_functions+=(powerline_precmd)
# }
# 
# if [ "$TERM" != "linux" ] && [ -f $(which powerline-go) ]; then
    # install_powerline_precmd
# fi

# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
source /usr/share/nvm/init-nvm.sh

alias mux=tmuxinator
alias zedit='vim ~/.zshrc'
alias src='source ~/.zshrc'

export ANDROID_HOME=/home/derek/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$HOME/scripts
export PATH=$PATH:$HOME/.emacs.d/bin

autoload -U compinit
compinit -i


function racd () {
    cd $(ra "$1")
}

function vcd () {
    cat $(ra --edit)
}
eval "$(starship init zsh)"
